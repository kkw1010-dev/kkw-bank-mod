# 작업 현황

## 이 모드가 하는 일

스카이림에 **홀드별 금융 시스템**을 넣는다. 은행이 아니라 **야를의 금고**이고,
장부는 행정관이 관리한다. 홀드 판별은 화자의 **범죄 팩션**으로 하며, 이는
스카이림 현상금 시스템이 쓰는 값과 같아서 관할이 게임과 일치한다.

- **예금** — 홀드별 계좌에 맡기고 찾는다
- **대출** — 야를의 금고에서 빌린다. 한도는 신용등급에 따른다
- **외상** — 잡화상에게 외상으로 거래한다. 홀드 단위로 합산된다
- **신용등급** — 도바킨의 행적으로 오르는 1~5단계. 대출·외상 한도를 모두 결정한다

거래 기록은 별도 탭이 아니라 예금·대출·외상 각 탭에 그 탭의 장부로 붙어 있다.

## 경로

| 용도 | 경로 |
|---|---|
| 저장소 | `C:\TAKEALOOK\BankPrismUI` |
| 원격 | `git@github.com:kkw1010-dev/kkw-bank-mod.git` |
| 배포 대상 (MO2) | `C:\TAKEALOOK\mods\BankPrismUI` |
| MO2 활성 프로필 | `TKL - MUNG ADDON` |
| 게임 데이터 | `C:\TAKEALOOK\Stock Game\Data\Skyrim.esm` |
| Papyrus 로그 | `C:\Users\kkw10\OneDrive\Documents\My Games\Skyrim Special Edition\Logs\Script\Papyrus.0.log` |
| SKSE 로그 | 같은 폴더의 `SKSE\skse64.log` |

Papyrus 로깅은 켜져 있다. 크래시 로그는 CrashLoggerSSE가 `SKSE\crash-*.log`로 남긴다.

## 빌드

빌드 절차와 각 단계의 함정은 [README.md](README.md)의 Building 절에 있다. 요약하면
**ESP 생성 → Papyrus 컴파일 → 배포** 세 단계이고, SKSE 플러그인은 C++를 건드릴
때만 다시 빌드하면 된다.

빌드 데몬이 남지 않도록 `MSBUILDDISABLENODEREUSE=1`과 `/nodeReuse:false`를 붙인다.

## 인게임에서 확인된 것

- 행정관 대화문(9홀드 전체), 잡화상 외상 대화문
- 은행 UI 표시, PrismaUI 연동, 탭 UI, 홀드별 배경 이미지
- 입금 / 출금, 거래 기록 표시
- 외상 거래 전체 흐름 (한도만큼 대여 → 바닐라 거래창 → 사용액만 채무 전환)
- 홀드별 합산 (벨레쏘어 + 루칸 → 화이트런 장부)
- 디버그 단축키 `-` (스캔코드 12)
- 화이트런 문구: 드래곤스리치 표기, 탭 제목, 대출 불가 안내, 금고 단계 설명
- 등급 전환(`set BankCreditPathWhiterun`)에 따른 대출 한도와 등급별 대사
- 부동산 담보: 설정·해제, 연체 7일 압류(집 셀 소유권이 화이트런으로), 완납 시 소유권 회복
- 대출 실행과 상환: 담보 테스트에서 3,000 대출 → 연체 부과 → 전액 상환까지 실제로 돌았다(로그)
- 배달부: 연체 1~7일차 독촉장이 하루 한 통씩 배달부 가방에 들어가고(로그), 배달부가 실제로
  가져온다. 한자리에서 시간을 보내면 여러 통을 한 번에 가져오는데, 바닐라 `WICourier`
  퀘스트가 장소 변경 이벤트(`CLOC`)로 출발하기 때문이다 — 버그가 아니다
- 독촉장 배달 (배달부가 실제로 가져옴)
- 새 회차에서 대화문 정상 (퀘스트 플래그를 바닐라와 맞춘 뒤)

## 빌드까지만 확인된 것

아래는 컴파일과 배포는 통과했으나 **인게임 동작을 확인하지 않았다.**

- **화이트런 한정 대화문 (행정관·잡화상)** — 한때 "확인됨"으로 적었으나 근거가 없었다. 조건을
  화이트런으로 좁힌 `1f801c2` 이후 네 세션의 Papyrus 로그 어디에도 `BankPrism: 은행 대화문 실행` /
  `외상 대화문 실행` 줄이 없다. 은행은 `-` 단축키로만 열렸다. 09-11 세션에서 행정관 선택지가
  뜨지 않는다는 보고가 들어왔다. `-`를 누르면 이제 `reach:` 줄과 화면 알림이 조건 충족 여부를 보여준다

- **연체 일 단위 부과** — 주 단위에서 일 단위로 바꾼 직후다. 1,000 대출(채무 1,200)
  후 8일차에 1,230, 9일차에 1,260이면 정상. 상한은 원금의 3배
- **독촉장 본문** — 63편 전체. 어느 것도 게임 안에서 열어보지 않았다
- **압류 시 정문 잠금과 열쇠 회수, 완납 시 복구** — 소유권 이전 위에 새로 얹었다
- **근저당 해지 수수료** — 감정가의 20%를 소지금·예금 순으로 차감. 완납이나 압류 해제 뒤에도
  근저당이 남는 것 포함
- **신용등급 전체** — 아래 "신용등급" 절의 12개 판정이 인게임에서 실제로 도는지.
  UI 쪽은 12개 등급 전부를 브라우저에서 넣어보고 확인했지만, 그 등급을 만들어내는
  Papyrus 판정은 게임 안에서만 확인할 수 있다
- **연대보증 (화이트런: 리디아)** — 세인 임명(`IsThaneOf(0)`) 시 활성화, 보증 설정(`BankGuarantorWhiterun=1`) 시 대출한도 +2,000G, 전액 상환 시 해제 가능, 연체 7일 시 구상권 청구(`BankGuarantorWhiterun=2`) 및 완납 시 원복. 리디아 사망 시 무효. 인게임 동작 확인 전
- **홀드별 문양·초상화가 게임 안에서 뜨는지** — 파일명을 ASCII로 바꾼 뒤 아직
  게임에서 보지 않았다. 초상화 폴백(`onerror`)도 마찬가지

## 신용등급

등급은 **행적으로 얻는 계급**이고 한 번 오르면 내려가지 않는다. 단계가 대출 한도와
외상 한도를 모두 정한다. 등급명은 무엇으로 올랐는지에 따라 달라진다 — 목록과 조건은
[UI_CONTRACT.md](PrismaUI/views/BankPrism/UI_CONTRACT.md)에 있다.

| 단계 | 대출 한도 | 외상 한도 |
|---|---|---|
| 1 외지인 | 불가 | 1,000 |
| 2 정체모를 용병 | 불가 | 1,500 |
| 3 | 5,000 | 2,000 |
| 4 | 12,000 | 3,500 |
| 5 | 25,000 | 6,000 |

### 판정에 쓰는 바닐라 레코드

**전부 진단 도구로 확인한 것이고, 기억으로 적은 것은 하나도 없다.** 두 개는 처음
짚었던 것이 틀렸다.

| 조건 | 레코드 | FormID |
|---|---|---|
| 세인 (홀드별) | `FavorJarlsMakeFriends`의 `<홀드>ImpGetOutofJail` / `<홀드>SonsGetOutofJail` | QUST 087E24 |
| 잡다한 목표 수 | `Game.QueryStat("Misc Objectives Completed")` | SkyrimSE.exe 문자열로 확인 |
| 드래곤 라이징 | MQ104 | 02610C |
| 알두인 1차 격퇴 | MQ206 Alduin's Bane | 036193 |
| 알두인 최종 처치 | MQ306 | 046EF3 |
| 컴패니언 가입 | C00 Take Up Arms | 04B2D9 |
| 서클 진입 | C03의 25단계 | 01CEF4 |
| 하빈저 | `CompanionsHarbingerFaction` | 1070DD |
| 대학 계급 | `CollegeofWinterholdFaction` 랭크 0~6 | 01F259 |
| 내전 입대 | `CWImperialFaction` / `CWSonsFaction` | 02BF9A / 02BF9B |
| 내전 계급 | `CW` 퀘스트의 `CWScript.PlayerRank` 1~4 | 019E53 |

**틀리기 쉬운 두 가지.**

- **세인 팩션은 없다.** `find Thane`이 팩션도 퍽도 0건을 낸다. 홀드별 `Favor25x`
  퀘스트가 있긴 하지만 야를이 임명하는 순간 `Stop()`으로 끝나서 단계 정보가
  사라진다. 바닐라가 나중까지 들고 있는 값은 `FavorJarlsMakeFriends`의 홀드별
  변수이고, 그것도 **야를의 범죄 팩션**으로 홀드를 가른다 — 이 모드가 쓰는 것과
  같은 손잡이다
- **내전 계급은 팩션 랭크가 아니다.** `CWImperialFaction`/`CWSonsFaction`은 랭크표가
  비어 있다. 그럴듯해 보이는 `CWCountMissionsDone` 글로벌은 바닐라 `CWScript`가
  주석으로 `DEPRECATED/OBSOLETE`라고 못박아 두었다. 살아 있는 값은 `PlayerRank`

이 때문에 `CWScript`와 `FavorJarlsMakeFriendsScript`를 직접 캐스팅해 읽는다.
그래서 바닐라 스크립트 전체가 컴파일 경로에 필요하다 (README Building 2절).

## 지금 범위: 화이트런만

화이트런에 집중하는 동안 나머지 여덟 홀드는 **꺼 두었다.** 계좌·대출 글로벌, 독촉장
63편, 배열 칸은 전부 그대로 생성되고 문만 닫혀 있다.

- **대화문** — 행정관 토픽은 `JobStewardFaction` AND 켜진 홀드 범죄 팩션의 멤버에게,
  외상 토픽은 켜진 홀드 잡화상의 판매 팩션에게만 뜬다. 화이트런 기준 각각 프로벤투스
  1명, 벨레쏘어·이솔다·카밀라·루칸 4명이다
- **Papyrus** — `HoldIsEnabled()`가 은행 창과 외상 거래 진입을 막는다. 꺼진 홀드의
  장부는 그대로 두되 연체 부과와 독촉장은 멈춘다. 디버그 단축키도 꺼진 홀드를 열지 않는다
- **빌드 검사** — `dotnet run`이 Skyrim.esm으로 두 토픽이 실제로 누구에게 뜨는지 계산해
  출력하고, 꺼진 홀드의 NPC가 끼거나 아무에게도 안 뜨면 실패한다. `HoldIsEnabled()`와
  생성기의 `enabledHolds`가 어긋나도 실패한다
- **다시 켜기** — `Program.cs`의 `enabledHolds`에 홀드 이름을, `HoldIsEnabled()`에 그
  인덱스를 더한다. 퀘스트 프로퍼티 구조는 바뀌지 않는다

화이트런 문안에서 야를의 거처를 가리키는 말(야를의 금고, 영지 금고, 궁정)은
**드래곤스리치**로 쓴다. 한국어 번역 `Skyrim_Korean.STRINGS`의 표기가 드래곤스리치이고
드래곤즈리치는 0건이다. 사람과 직책(야를께서, 영주 발그루프, 궁정 마법사)은 그대로 둔다.
UI는 `BankView.html`의 `SEAT_TEXT`로 홀드별로 덮어쓰므로 공용 문구는 바뀌지 않았다.

## 다음 작업

### 0. UI 꾸미기

`PrismaUI/views/BankPrism/BankView.html`의 디자인 작업. 게임과의 통신 규약은
[UI_CONTRACT.md](PrismaUI/views/BankPrism/UI_CONTRACT.md)에 정리되어 있다. 그
계약만 지키면 레이아웃·색·탭 구성은 자유롭게 바꿔도 된다. Papyrus나 C++는 건드릴
필요가 없다.

남은 그림 작업: 행정관 초상화와 잡화상 초상화 모두 화이트런 5장씩뿐이다. 다른
홀드는 `portraits/<holdKey>-t<단계>.jpg`와 `portraits/merchant-<holdKey>-t<단계>.jpg`를
다섯 장씩 넣으면 그것으로 바뀌고, 없는 동안은 화이트런 것을 대신 쓴다. 홀드별
행정관 이름은 `BankView.html`의 `STEWARDS`에, 잡화상은 `MERCHANTS`에 한 줄씩
추가한다. 금고 그림 `vault/vault-t<단계>.jpg`는 홀드와 무관하게 예금액으로만
고른다.

### 1. 잡화상 대사의 홀드별 문안

`BankView.html`의 `MERCHANT_QUOTES`와 `MERCHANTS`에는 화이트런(벨레소어)만 있고,
나머지 8홀드도 같은 대사를 쓴다. 그래서 윈터홀드 잡화상과 대화해도 대사는
"이 벨레소어의 가게", "우리 화이트런의 귀하신 종사"라고 말한다. 잡화상 명단은
`dotnet run -- credit`으로 뽑는다.

독촉장 63편은 9홀드 전부 작성되어 있다. `DunningLetter()`의 범용 폴백은 이제
어느 홀드에서도 쓰이지 않지만, 홀드가 늘어날 때를 대비해 남겨 두었다.

### 2. 담보의 나머지 두 갈래

부동산과 보증인은 게임과 연결되었고 현물은 탭에 "준비 중"으로 남아 있다. 조사 및 구현 결과:

- **보증인** — 화이트런 리디아(`HousecarlWhiterun`) 구현 완료(빌드 검증). 처음부터
  `PlayerHousecarlFaction`과 `PlayerFaction` 소속이라 팩션으로는 임명 여부를 알 수 없어,
  화이트런 세인 판정(`IsThaneOf(0)`)을 조건으로 쓴다. 보증 시 2,000G 한도 증액,
  연체 7일 시 구상권 청구, 완납 시 원복. 리디아 사망 시 무효
- **현물** — 숨은 보관 상자를 바닐라 상자 창으로 열고, 닫힐 때 SKSE `GetNumItems` /
  `GetNthForm` / `GetGoldValue`로 합산한다. 외상과 같은 모양이라 같은 크래시 위험을 안는다.
  `GetGoldValue`는 기본값이라 인챈트·개량분은 잡히지 않는다

### 3. 서드파티 채권 모드 연동

UI의 "채권 매각" 버튼은 지금 안내 문구만 띄운다. 대상 모드의 이름과, 훅을 걸
이벤트나 스크립트 명칭이 필요하다.

### 4. 남은 판단거리

- **독촉장 서명** — 지금은 "○○ 야를의 행정관"로 직책만 쓴다. 내전으로 행정관이
  바뀌는 홀드가 셋이고(리치 라에렉↔레부루스, 하얄마치 아슬푸르↔팍투르, 팔크리스
  네냐↔테클라) 페일에는 행정관이 없어서, 이름을 박으면 세이브 상당수에서 틀린다.
  화이트런 1일차에 발그루프 이름이 들어 있었으나, 형식을 나머지와 맞추면서 빠졌다.
  지금은 63편 전부 직책으로만 서명한다
- **거래 중 크래시** — 외상 거래창이 열린 채 게임이 죽으면 빌려준 골드가 회수되지
  않는다. 정산이 `OnMenuClose`에 걸려 있어서다. 실제로 문제가 되면 로드 시
  미회수분을 정리하는 안전장치를 넣는다

## 설계 결정과 근거

되돌리기 전에 왜 이렇게 됐는지 알아야 하는 것들.

- **외상은 임시 대여 + 바닐라 거래창.** 상인 재고를 복제하면 재입고 동기화, 화술
  퍽 가격, 상인 소지금, 판매 방향 거래가 전부 깨진다. 한도만큼 골드를 빌려주고
  거래창을 닫을 때 실제 사용액만 채무로 돌린다
- **대출은 서명 시 20% 일시 가산.** 만기가 7일이라 복리와 체감이 같다(일 3% 복리
  = 7일에 23%). 복리는 상한 관리와 오버플로 위험만 늘린다
- **만기 7일.** 상점 재입고 2일, 던전 리스폰 10일 사이. 던전 하나 다녀올 시간
- **연체는 일 단위.** 주 단위였을 때 독촉장은 매일 오는데 숫자가 일주일간 그대로라
  고장난 것처럼 보였다
- **연체 제재는 독촉장, 현상금 아님.** 바닐라 배달부(`WICourierScript`)를 쓴다
- **대출·외상 한도는 신용등급.** 한때 메인 퀘스트 진행도만 봤는데, 그러면 컴패니언
  하빈저도 아크메이지도 야를에게는 이름 없는 칼잡이였다. 지금은 네 갈래 행적
  (메인 퀘스트 · 컴패니언 · 마법대학 · 내전)과 홀드별 세인이 같은 사다리를 오른다
- **등급은 내려가지 않는다.** 상환 행적으로 오르내리는 점수였을 때는 연체 한 번에
  야를의 예우가 사라져서, 계급이 아니라 신용조회처럼 읽혔다. 지금은 홀드별로 처음
  인정받은 단계와 그 경로를 글로벌에 박아두고 올라가기만 한다
- **세인은 퀘스트가 아니라 `FavorJarlsMakeFriends`의 변수로 판정.** 근거는 위 참조
- **외상 대상은 건물 있는 잡화상 9곳.** 대장장이·연금술사·여관·노점·카짓 대상은
  제외했다. 이오를룬드가 하빈저에게 외상을 주면 몰입이 깨진다
- **대화문 조건은 팩션.** NPC 개체를 지목하면 NPC 오버홀 모드에서 어긋난다
- **부동산 담보는 등급과 무관하게 한도를 더한다.** 이름 없는 칼잡이의 말은 믿지 않아도
  집문서는 받는다. 인정비율 60%(`BankCollateralLtvPercent`), 브리즈홈이면 3,000
- **압류는 연체 7일(`BankForecloseOverdueDays`), 마지막 독촉장과 같은 날.** 집 셀의 소유자를
  홀드 범죄 팩션으로 바꾸기만 하고 물건은 건드리지 않는다. 바닐라가 구입 때 같은 셀을
  `PlayerFaction` 소유로 바꾸는 것의 반대 방향이라, 완납하면 그대로 되돌릴 수 있다
- **압류하면 집에 들어갈 수 없다.** 정문(`0001A6F9`, 퍼시스턴트)을 "열쇠 필요"로 잠그고 열쇠
  (`093B08`)를 회수한다. 소유권 이전도 그대로 두어, 문을 거치지 않고 들어가도 물건은 영지
  소유다. 압류 순간 플레이어가 집 안에 있으면 가두지 않고, 나온 뒤 다음 점검이나 은행 방문 때
  잠근다. 안쪽 문은 건드리지 않는다. 원래 잠금 상태와 가져간 열쇠 수를 기억했다가 그대로 돌려준다
- **근저당은 해지할 때만 수수료를 낸다.** 감정가의 20%(`BankLienReleaseFeePercent`), 브리즈홈이면
  1,000. 설정은 무료다. 대출을 갚아도, 압류가 풀려도 근저당은 남고 해지는 따로 신청한다
- **집에 대한 사실은 저장하지 않고 바닐라에서 읽는다.** 소유 여부는
  `HousePurchaseScript.WhiterunHouseVar`(바닐라 하스파이어도 이것으로 판정), 감정가는
  `HPWhiterun`, 셀은 구입 프래그먼트 스크립트의 `WhiterunHouse`
- **연대보증은 하우스칼이 서고, 한도를 더한다.** 화이트런은 리디아(`000A2C94`). 세인 임명(`IsThaneOf(0)`) 시 자격 획득, 기본 2,000 골드 증액(`BankGuarantorCreditWhiterun`). 별도 보증 수수료는 없다.
- **리디아는 처음부터 플레이어 팩션 소속이다.** 팩션 검사로는 세인/하우스칼 배속 여부를 알 수 없으므로 바닐라 `FavorJarlsMakeFriendsScript.IsThaneOf(0)`로 판정한다.
- **연체 7일 시 구상권 청구.** 압류와 같은 날(`BankForecloseOverdueDays`)에 보증 상태가 2(구상권 청구)로 전환된다. 대출을 전액 상환하면 정상 보증(1)으로 복원된다. 보증 해지는 대출이 완전히 없을 때만 가능하다.

## 진단 도구

`EspGenerator`에 조사용 모드가 들어 있다. **구조를 추측하지 말고 게임 데이터에서
직접 확인할 때 쓴다.** 지금까지 막힌 문제는 거의 전부 여기서 풀렸다.

```
dotnet run                  # 생성 후 디스크에서 다시 읽어 레코드 구조를 출력
dotnet run -- compare       # 바닐라의 정상 대화문 구조
dotnet run -- ordiff        # 바닐라 OR 조건과 우리 것을 나란히
dotnet run -- vendors       # 상인 팩션과 소속 인원
dotnet run -- shops         # 상점 창고가 실내인지 실외인지
dotnet run -- credit        # 외상 대화문이 실제로 닿는 NPC 명단
dotnet run -- general       # 잡화상 판매 목록 계열
dotnet run -- stewards      # 홀드별 행정관
dotnet run -- standing      # 메인 퀘스트, 세인 팩션, 배달부 퀘스트
dotnet run -- notes         # 바닐라 쪽지의 필수 필드
dotnet run -- gmst          # 재입고·리스폰 등 게임 설정
dotnet run -- npc <이름>    # NPC의 FormID와 소속 판매 팩션
dotnet run -- ranks         # 팩션 소속 랭크
dotnet run -- find <말> [qust|fact|glob|perk]
                            # EditorID로 레코드 찾기. 팩션은 랭크표까지, 글로벌은 값까지
dotnet run -- quest <폼ID>  # 퀘스트의 단계·로그문구·목표·붙은 스크립트
dotnet run -- info <폼ID>   # 대화 INFO 하나의 조건 전부
dotnet run -- usage <폼ID>  # 그 레코드를 읽는 바닐라 대화 조건과 RunOn 종류
dotnet run -- script <이름> # 그 스크립트가 붙어 있는 퀘스트
```

뒤의 넷은 신용등급 작업에서 추가했다. `find`는 "세인 팩션이 없다"를, `usage`는
"이 팩션을 플레이어에게 실제로 쓰는가"를, `script`는 "이 스크립트를 어느 퀘스트에
캐스팅해야 하는가"를 각각 추측 없이 답한다.

인자 없이 실행하면 플러그인을 생성한 뒤 **디스크에서 다시 읽어** 글로벌, 퀘스트
프로퍼티, 대화문 조건과 그 플래그, 폼리스트, BOOK의 모델까지 출력한다. 대화문이
안 뜨거나 게임이 죽는 문제는 대부분 이 출력에서 잡힌다.

## 겪었던 함정

같은 문제를 반복하지 않기 위해 기록한다. **전부 실제로 시간을 잡아먹은 것들이다.**

- **Papyrus 소스는 UTF-8 + BOM으로 저장한다.** BOM 없는 UTF-8은 컴파일러가 시스템
  ANSI로 읽어, 컴파일 성공을 보고하면서 한글을 다른 한자로 바꿔놓는다
- **레코드 FormID는 고정 테이블에서 배정한다.** 순서대로 배정하면 레코드를 추가할
  때 뒤의 것이 전부 밀려서, 실행 중인 퀘스트가 세이브에서 어긋나고 대화문이 통째로
  사라진다
- **플레이어 대화문은 TopLevel DialogBranch에 속해야 한다.** 브랜치가 없으면 대화
  메뉴에 후보로 오르지도 않는다
- **OR 그룹은 마지막 조건에도 OR 플래그를 붙인다.** 마지막만 비우면 엔진이
  `(나머지 중 하나) AND (그것)`으로 읽어 아무도 만족하지 못한다. 바닐라 INFO
  000E3D가 그 증거다
- **BOOK에는 모델이 있어야 한다.** 없으면 책을 여는 순간 `BookMenu`에서 널 참조로
  죽는다. 바닐라 쪽지(`WERoad08CourierLetter`)의 필드를 베낀다
- **본문 맨 앞의 `[pagebreak]`는 빈 첫 페이지를 만든다**
- **Papyrus에서 `key`는 변수명으로 쓸 수 없다.** 기존 스크립트 타입과 겹친다
- **PrismaUI `Focus()`의 세 번째 인자는 false여야 한다.** PrismaUI가 자체 메뉴로
  키보드를 캡처하므로, 끄면 클릭만 되고 입력이 막힌다
- **게임 일시정지 중에는 키보드 입력이 뷰에 전달되지 않는다.** 그래서 금액은
  타이핑이 아니라 버튼으로 고른다
- **글로벌이 늘면 기존 세이브와 호환되지 않는다.** 프로퍼티 구조가 바뀔 때는 새
  회차가 필요하다
- **바인딩되지 않은 배열 프로퍼티는 읽는 순간 예외가 난다.** `arr == None`으로
  막을 수 없다. 배열과 같은 시점에 추가된 **스칼라** 프로퍼티를 준비 상태 플래그로
  삼아야 한다 (`HoldsReady()`, `LoansReady()`)
- **바닐라 스크립트 전체를 임포트 경로에 넣을 때는 SKSE 폴더를 앞에 둔다.**
  크리에이션 킷의 `Form.psc`/`Quest.psc`에는 SKSE 확장이 없어서, 바닐라 트리가 먼저
  잡히면 `RegisterForModEvent`·`RegisterForKey`·`RegisterForMenu`가 통째로 사라지고
  모드 스크립트가 전부 컴파일에 실패한다
- **UI 그림 파일명은 ASCII로 둔다.** 게임 안 브라우저에서 그림이 안 뜨는 이유는
  확인할 방법이 없다. 홀드 키(`whiterun` 등)가 따로 있는 이유가 이것이다
- **대화문을 제공하는 퀘스트는 플래그 `0x011`, 종류 `None`이어야 한다.** 우리가
  `0x001` / `Misc`였을 때 퀘스트는 정상 실행되고 스크립트도 붙고 Papyrus 오류도
  없는데 대화문만 아무에게도 뜨지 않았다. `0x010` 비트는 Mutagen에도 xEdit에도
  이름이 없지만 바닐라 대화 퀘스트는 전부 켜두고 있다
- **은행 창이 `-` 단축키로 열리는 것은 대화문 검증이 아니다.** 대화문은 로그의
  `BankPrism: 은행 대화문 실행` / `외상 대화문 실행` 줄로만 확인한다. 단축키로 연 세션을 대화문
  확인으로 적었다가, 조건을 바꾼 뒤 네 세션 동안 대화문이 한 번도 선택되지 않은 것을 놓쳤다
- **행정관은 `JobStewardFaction`을 기본 레코드에 랭크 -1(비소속)로 들고 있다.** 소속은 런타임에
  마을 대화 퀘스트의 Steward 별칭이 준다(`dotnet run -- aliasfaction JobStewardFaction`: 화이트런은
  `DialogueWhiterun` 별칭 14, 프로벤투스 고정). 그래서 행정관 토픽은 그 바닐라 별칭이 채워져 있을
  때만 뜬다. 빌드의 도달 범위 검사는 이제 랭크 -1을 비소속으로 치고, 별칭에 기대는 도달은
  "런타임 별칭 의존"으로 표시한다
- **`RegisterForSingleUpdateGameTime`의 인자는 게임 시간 "시" 단위다.** 바닐라
  `Form.psc` 주석이 "in afInterval hours of game time"이라고 적어 둔다. `1.0`을 넘겨
  하루 한 번이라고 믿는 동안, 연체된 대출은 게임 시간 한 시간마다 배달부 가방에
  독촉장을 한 통씩 넣었다. 게다가 한 번만 받는 등록은 대기 중인 등록을 덮어쓰므로,
  은행을 열 때마다 재등록하면 점검이 매번 하루씩 밀린다. 그래서 점검 예정 시각을
  스크립트 변수에 두고 그 시각이 지났을 때만 다시 건다

## 작업 방식

- 게임 데이터에 대한 판단은 **추측하지 말고 진단 도구로 확인한다.** 위 함정 목록은
  대부분 바닐라 레코드와 대조해서 찾아낸 것이다
- ESP를 바꿨으면 **디스크에서 다시 읽어 검증한다.** 인자 없는 `dotnet run`이 그
  일을 한다
- 인게임 동작은 사용자만 확인할 수 있다. **빌드 통과와 인게임 검증을 구분해서
  보고한다**
