# 작업 현황

빌드 절차는 [README.md](README.md)의 Building 절을 참고한다.

## 인게임에서 확인된 것

플레이 중에 실제로 동작하는 것을 확인한 항목이다.

- 프로벤투스 아베니치의 **은행 대화문**, 벨레쏘어의 **외상 대화문**
- 은행 UI 표시 및 PrismaUI 연동
- **입금 / 출금** 동작
- **거래 기록** 표시 (이번 세션 한정, 세이브에 기록하지 않음)
- **디버그 단축키** (Insert) 로 어디서든 은행 열기

## 빌드까지만 확인된 것

컴파일과 배포는 통과했으나 인게임 동작은 아직 확인하지 않은 항목이다.

- **외상 거래 전체 흐름**: 한도만큼 골드 대여 → 바닐라 거래창 → 창을 닫을 때
  사용액만 외상금으로 전환하고 나머지 회수

## 다음 작업

### 1. 외상 거래 흐름 인게임 테스트

벨레쏘어에게 "외상으로 거래하고 싶습니다"를 선택했을 때 확인할 것:

1. 거래창이 열린 시점에 소지금이 한도(기본 1000 G)만큼 늘어 있는가
2. 300 G어치를 사고 닫으면 소지금이 원래대로 돌아오고 외상금이 300 G인가
3. 아무것도 사지 않고 닫으면 소지금이 정확히 원래대로인가
4. 물건을 **판매**했을 때 그 대금은 회수되지 않고 남는가

### 2. 알려진 한계: 거래 중 크래시 시 대여금이 남는다

정산이 `OnMenuClose`에 걸려 있어, 거래창이 열린 상태에서 게임이 종료되면
빌려준 골드가 회수되지 않는다. 실제로 문제가 되면 게임 로드 시 미회수분을
정리하는 안전장치를 넣는다. 플레이어 별칭(alias)에 `OnPlayerLoadGame`을 두고
`bCreditBarterActive`가 남아 있으면 회수하는 방식이 된다.

### 3. 외상 범위 확대

현재는 벨레쏘어 한 명이다. 넓힐 때의 기준은 이미 조사해 두었다.

Skyrim.esm 기준 Vendor 플래그 팩션 145개 중 소속 NPC가 있는 것이 109개,
NPC 합계 197명이다. 이 중 **상점 창고(MerchantContainer)를 가진 105개 팩션
138명**이 "가게를 가진 상인"에 해당한다. 창고가 없는 4개 팩션이 동료
(`CurrentFollowerFaction`, 40명)와 떠돌이 사냥꾼(`WEServicesHunterFaction`,
17명)이므로, 창고 보유 여부만으로 걸러진다. 손으로 관리할 목록은 필요 없다.

조건은 이미 `GetInFaction`으로 되어 있으므로, 팩션마다 OR 조건을 하나씩
붙이면 된다. 목록은 생성기가 Skyrim.esm에서 직접 뽑는다.

`dotnet run -- vendors` 로 언제든 다시 조사할 수 있다.

### 4. 외상 한도와 연체 페널티 기획

정해야 할 것:

- 한도를 무엇으로 결정할지 (고정값 / 화술 레벨 / 상인 호감도 / 예금 잔고 연동)
- 상환 기한과 연체 시 발생하는 결과
- 외상이 상인별로 따로 쌓이는지, 지금처럼 하나로 합산되는지

현재는 `MerchantCreditLimit` 글로벌 하나에 고정 1000 G이고, 외상금도
`MerchantCreditDebt` 하나로 합산된다. 상인별로 나눈다면 글로벌 하나로는
부족하므로 저장 구조부터 다시 잡아야 한다.

### 5. 서드파티 채권 모드 연동

UI의 "채권 매각" 버튼은 지금 안내 문구만 띄운다. 연동하려면 대상 모드의
이름과, 훅을 걸 이벤트나 스크립트 명칭이 필요하다.

## 진단 도구

생성기에 조사용 모드를 넣어 두었다. 구조를 추측하지 말고 게임 데이터에서
직접 확인할 때 쓴다.

```
dotnet run -- compare        # 바닐라의 정상 대화문 구조를 덤프
dotnet run -- vendors        # 상인 팩션과 소속 인원 집계
dotnet run -- npc <이름>     # NPC의 FormID와 소속 판매 팩션
dotnet run -- belethor       # 벨레쏘어에게 붙는 바닐라 대화문
```

인자 없이 실행하면 플러그인을 생성한 뒤 디스크에서 다시 읽어 레코드 구조를
출력한다. 대화문이 안 뜨는 문제는 대부분 여기서 잡힌다.

## 겪었던 함정

같은 문제를 반복하지 않기 위해 기록한다.

- **Papyrus 소스는 UTF-8 + BOM으로 저장한다.** BOM 없는 UTF-8은 컴파일러가
  시스템 ANSI로 읽어, 컴파일 성공을 보고하면서 한글을 다른 한자로 바꿔놓는다.
- **레코드 FormID는 고정 테이블에서 배정한다.** 순서대로 배정하면 레코드를
  추가할 때 뒤의 것이 전부 밀려서, 실행 중인 퀘스트가 세이브에서 어긋난다.
- **플레이어 대화문은 반드시 TopLevel DialogBranch에 속해야 한다.** 브랜치가
  없으면 대화 메뉴에 후보로 오르지도 않는다.
- **Papyrus에서 `key`는 변수명으로 쓸 수 없다.** 기존 스크립트 타입과 겹친다.
- **PrismaUI의 `Focus()` 세 번째 인자(`disableFocusMenu`)는 false여야 한다.**
  PrismaUI가 자체 메뉴로 키보드를 캡처하므로, 끄면 클릭만 되고 입력이 막힌다.
