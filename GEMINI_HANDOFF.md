# BankPrismUI 작업 인계 프롬프트 (제미나이용)

아래 전체를 그대로 붙여 넣으십시오.

---

당신은 `C:\TAKEALOOK\BankPrismUI`에서 스카이림 SE 모드 작업을 이어받습니다. 이 모드는
홀드별 금융 시스템(예금·대출·외상·근저당)을 PrismaUI로 구현합니다. 원격 저장소는
`git@github.com:kkw1010-dev/kkw-bank-mod.git`, 브랜치는 `master`입니다.

## 0. 시작 전에 읽을 것 (순서대로)

1. `TASK.md`: 경로, 인게임 확인 현황, 설계 결정의 근거, 겪었던 함정, 진단 도구
2. `README.md`의 Building 절: 빌드 명령과 각 단계의 함정
3. `PrismaUI/views/BankPrism/UI_CONTRACT.md`: UI를 건드린다면 반드시 읽습니다
4. `STRINGS.md`: 게임에 뜨는 문구 목록

`GEMINI_TASK.md`는 예전 지시서라 내용이 낡았습니다. 참고하지 마십시오.
작업 전에 `git log --oneline -15`로 최근 변경을 확인하십시오.

## 1. 반드시 지킬 규칙

이 규칙들은 전부 실제로 시간을 낭비한 경험에서 나왔습니다.

**게임 데이터는 추측하지 않습니다.** FormID, EditorID, 팩션 소속, 퀘스트 스테이지, 문,
열쇠, 셀처럼 게임 데이터에 대한 판단은 반드시 `EspGenerator`의 진단 모드로 확인합니다.
기억이나 위키로 채우지 마십시오. 지금까지 막힌 문제는 거의 전부 여기서 풀렸습니다.
```
cd EspGenerator
dotnet run -- find <말> [qust|fact|glob|perk]  # EditorID로 레코드 찾기
dotnet run -- factions <NPC>        # 소속 팩션·랭크, 엔진이 쓰는 범죄 팩션
dotnet run -- members <팩션,팩션>    # 나열한 팩션에 모두 속한 NPC (대화 조건 도달 범위 확인용)
dotnet run -- refs <NPC>            # 배치 레퍼런스 ID와 셀 (콘솔 player.moveto에 쓰는 ID)
dotnet run -- qfrag <퀘스트폼ID>     # 시작 이벤트, 별칭, 스테이지별 프래그먼트, 프로퍼티 실제 값
dotnet run -- doors <셀EditorID>     # 셀 안의 문과 셀로 들어오는 문, 잠금·열쇠
dotnet run -- quest/info/usage/script/credit/stewards ...   # TASK.md 진단 도구 절 참고
```
필요한 진단이 없으면 `Program.cs`에 진단 모드를 추가해서 확인하십시오.

**할 수 있는 일은 직접 합니다.** 사용자에게 "이 순서대로 해 보세요" 같은 수동 절차를 떠넘기지
마십시오. 빌드, 배포, 검증, 커밋은 명령으로 할 수 있습니다. 사용자에게 요청할 것은 게임을
직접 실행해야만 볼 수 있는 동작 확인뿐입니다. 시도가 실패했다면 실제 오류 문구를 먼저 보고한
뒤에 대안을 제시하십시오. 조용히 다른 방법으로 넘어가지 마십시오.

**빌드 통과와 인게임 검증을 구분해서 보고합니다.** "컴파일되고 배포됐다"는 동작한다는 증거가
아닙니다. 보고는 항상 세 칸으로 나눕니다.
- 게임 밖에서 검증한 것
- 게임에서만 확인할 수 있어서 아직 모르는 것
- 로그로 확인할 수 있는 것

**대화문이 뜨는지는 매번 로그로 확인합니다.** 이 모드는 대화문이 조용히 사라지는 사고를 여러 번
겪었습니다(퀘스트 플래그, OR 플래그, FormID 이동, 조건 변경). 오류도 로그도 남지 않아서 사용자가
게임을 켜서 NPC에게 걸어가 봐야만 알 수 있었습니다.
- 은행 창이 `-` 단축키로 열리는 것은 **대화문 검증이 아닙니다.** 단축키는 대화문을 거치지 않습니다.
- 무엇을 바꿨든, 사용자에게 테스트를 요청할 때는 확인 항목 **맨 앞**에 이것을 두십시오: "프로벤투스에게
  `은행 업무를 보고 싶습니다`를 **골라** 보고 로그에 `BankPrism: 은행 대화문 실행`이 있는지, 벨레쏘어에게
  외상 선택지를 골라 `BankPrism: 외상 대화문 실행`이 있는지".
- `-` 키를 누르면 `reach:` 줄과 화면 알림(`행정관 대화 조건 충족/불충족`)이 지금 대화 조건이 맞는지
  보여줍니다. 사용자가 "대화문이 안 뜬다"고 하면 이 줄부터 읽으십시오.
- 대화 조건(`AddTopic`의 `required`/`anyOf`), `DIAL`/`INFO`/`DLBR` 레코드, 퀘스트 플래그, 기존 FormID는
  **사용자 지시 없이 바꾸지 마십시오.** 바꿨다면 커밋 메시지와 보고 첫 줄에 적으십시오.
- 행정관의 `JobStewardFaction`은 기본 레코드에서 랭크 -1(비소속)이고, 바닐라 `DialogueWhiterun`의
  Steward 별칭이 런타임에 부여합니다. 빌드 검사의 "도달 범위"는 이 의존을 표시할 뿐 게임 상태를
  보장하지 않습니다.

**실패는 스스로 드러나게 만듭니다.** 사용자가 게임을 켜서 고장을 직접 찾아내는 일을 반복하게
하지 마십시오. 조용히 깨지는 조건(오류도, 로그도, 화면 표시도 없는 경우)은 빌드 단계의
검사로 막고, Papyrus에는 `BankPrism:` 접두어로 `Trace`를 남기십시오. 검사를 추가했다면
일부러 망가뜨려서 실제로 잡아내는지 확인하고, 원상 복구한 뒤 결과를 보고하십시오.

**ESP를 바꾸면 새 회차가 필요한지 보고합니다.** 퀘스트 프로퍼티나 글로벌이 늘면 기존
세이브와 맞지 않습니다. 사용자는 매번 새 회차로 테스트하므로 이것은 경고가 아니라 한 줄짜리
사실입니다. 빠뜨리지만 말고 짧게 적으십시오. 레코드 FormID는 `Program.cs`의 고정 테이블에서만
배정하고, 기존 ID는 절대 옮기지 않습니다.

**인게임 문구는 짧은 행정 문체로 씁니다.** 사용자가 최종 채택하는 문안은 노트북LM 초안에 가까운
짧은 문체입니다. 긴 문어체로 "개선"하지 마십시오. 형식이 어긋난 문안이 있으면 다수의 형식에
맞춥니다. 장소는 **드래곤스리치**(한국어 번역 표기)로, 직책은 **행정관**으로 씁니다.
잡화상 대사의 홀드별 문안은 사용자가 노트북LM으로 따로 작업하므로 손대지 마십시오.

**프로세스를 남기지 않습니다.** 빌드에는 `MSBUILDDISABLENODEREUSE=1`과 `/nodeReuse:false`를
붙이고, 끝나면 `dotnet build-server shutdown`을 실행합니다. UI 테스트용 `python -m http.server`를
띄웠다면 끝난 뒤 윈도우 도구(`Get-CimInstance Win32_Process` + `Stop-Process`)로 종료하고,
포트가 닫혔는지까지 확인하십시오. Git Bash의 `ps`/`pkill`은 윈도우 프로세스를 보지 못해서
"죽었다"고 잘못 읽힙니다.

**커밋은 하되, 푸시는 사용자가 말할 때만 합니다.** 커밋 메시지에는 무엇을 왜 바꿨는지와
"검증한 것 / 게임에서 안 본 것"을 적으십시오.

## 2. 빌드와 검증

1. **ESP:** `cd EspGenerator && dotnet run`
   - 생성한 뒤 디스크에서 다시 읽어 검사하고, 하나라도 실패하면 exit 1로 끝납니다. 실패하면 배포하지 마십시오.
   - 대화문 불변조건
   - 홀드 제한: 대화 토픽이 Skyrim.esm 기준으로 누구에게 뜨는지 계산하고, `HoldIsEnabled()`와 `enabledHolds`가 일치하는지 봄
   - 폰트: ESP 문자열과 Papyrus 문자열 리터럴 전부
2. **Papyrus:** README의 컴파일 명령을 씁니다. 임포트 경로에서 **SKSE 폴더가 `Scripts\Vanilla`보다 앞**이어야 합니다.
   - `.psc`는 **UTF-8 BOM + CRLF**로 저장합니다. BOM이 없으면 컴파일은 성공하는데 한글이 깨집니다.
3. **UI:** `BankView.html`을 로컬 서버로 띄워 브라우저에서 `window.bankUpdateParams(...)`에 payload를 넣어 봅니다. 상태별 표시, 버튼이 보내는 `bankAction`, 스크립트 오류를 확인하십시오.
4. **배포:** `powershell -ExecutionPolicy Bypass -File deploy.ps1`. 끝나면 배포본(`C:\TAKEALOOK\mods\BankPrismUI`)과 저장소의 해시가 같은지 확인합니다.
5. **로그:** `C:\Users\kkw10\OneDrive\Documents\My Games\Skyrim Special Edition\Logs\Script\Papyrus.0.log`
   - `BankPrism:`으로 검색합니다.
   - `Suspended stack count is over`가 보이면 다른 모드 때문에 VM이 포화된 것입니다. 이때는 은행 창 갱신이 멈춥니다.

## 3. 현재 상태 (커밋 `0584e8b` 기준)

**범위:** 화이트런만 켜져 있습니다. 나머지 8홀드는 데이터를 남긴 채 대화문과 기능만 막았습니다.
- 행정관 토픽은 프로벤투스, 외상 토픽은 벨레쏘어·이솔다·카밀라·루칸에게만 뜹니다.
- 다시 켜려면 `Program.cs`의 `enabledHolds`와 Papyrus의 `HoldIsEnabled()`를 함께 고칩니다. 빌드 검사가 둘의 일치를 확인합니다.

**인게임에서 확인된 것:**
- 드래곤스리치 문구
- 디버그 단축키 `-`
- 등급 전환에 따른 한도와 대사
- 대출 실행·연체 부과·상환
- 독촉장 배달: 하루 한 통씩 가방에 들어갑니다. 한자리에서 기다리면 몰아서 오는 것은 바닐라 `WICourier`가 장소 변경(`CLOC`)으로 출발하기 때문이며, 버그가 아닙니다.
- 부동산 근저당: 설정, 연체 7일 압류(집 셀 소유권 이전), 완납 시 소유권 회복

**빌드까지만 된 것 (게임에서 아직 안 봄):**
- **행정관·잡화상 대화문.** `1f801c2` 이후 로그에 대화 프래그먼트 실행 기록이 0건이고, 09-11에는 행정관
  선택지가 안 뜬다는 보고가 있었습니다. 원인 확정 전입니다
- 연대보증(리디아)
- 압류 시 브리즈홈 정문(`0001A6F9`) 잠금과 열쇠(`093B08`) 회수, 완납 시 복구
- 압류 순간 플레이어가 집 안에 있으면 나온 뒤 잠그는 동작
- 근저당 해지 수수료: 감정가의 20% = 1,000 G, 소지금 → 예금 순서로 차감
- 완납이나 압류 해제 뒤에도 근저당이 남는 것
- 리디아(하우스칼, 브리즈홈 거주)가 문이 잠겼을 때 어떻게 행동하는지

## 4. 남은 작업 후보

사용자가 지시하는 것부터 하십시오. 아래는 목록일 뿐 우선순위가 아닙니다.

- **보증인 담보:** 리디아는 처음부터 `PlayerHousecarlFaction`/`PlayerFaction` 소속이라 팩션으로는 임명 여부를 알 수 없습니다. 이미 쓰는 화이트런 세인 판정(`IsThaneOf`)을 조건으로 쓰는 방향입니다.
- **현물 담보:** 숨은 상자를 바닐라 상자 창으로 열고, 닫힐 때 SKSE `GetNumItems`/`GetNthForm`/`GetGoldValue`로 합산하는 방향입니다. 외상과 같은 크래시 위험이 있고, 인챈트 가치는 합산에 빠집니다.
- **배달부:** 아직 배달되지 않은 옛 독촉장은 빼고 최신 한 통만 가져오게 할지. 사용자 결정이 필요합니다.
- **나머지 과제:** 행정관·잡화상 초상화(현재 화이트런 것만 있음), 채권 매각 서드파티 연동, 외상 거래 중 크래시 시 대여금 회수, 기존 세이브에서 스크립트 교체 후 배달 점검을 재시작하는 로드 이벤트 별칭(`OnPlayerLoadGame`)

## 5. 이 코드베이스에서 특히 조심할 것

- 게임 시간 등록(`RegisterForSingleUpdateGameTime`)의 인자는 **시간 단위**입니다. 1회 등록은 대기 중인 등록을 덮어씁니다.
- 바인딩되지 않은 배열 프로퍼티는 읽는 순간 예외가 납니다. 같은 시점에 추가한 스칼라 프로퍼티를 준비 상태 플래그로 씁니다.
- 대화 조건의 OR 그룹은 마지막 조건에도 OR 플래그를 붙여야 합니다.
- UI에서 금액은 버튼으로만 받습니다(일시정지 중에는 키보드 입력이 전달되지 않음). `alert()`와 외부 리소스는 쓰지 않고, 그림 파일명은 ASCII로 합니다.
- 대화 조건은 NPC 개체가 아니라 팩션으로 겁니다. NPC 오버홀 모드와 호환되게 하기 위해서입니다.
- 게임 로직의 권위는 Papyrus에 있습니다. UI의 검사는 이유를 빨리 보여주는 용도일 뿐입니다.
